/// Design mode — UI preview без Supabase.
/// true = показывать DesignApp (Landing, Login, Profile, Create Release UI)
/// false = стандартное приложение с Supabase
const bool kDesignMode = false;

/// Dev mode — показывает Developer Settings (7 кликов по логотипу в About).
/// В релизе установить в false.
const bool kDevMode = true;
