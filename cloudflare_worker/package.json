{
  "name": "wandering-snow-3f00",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check:api-contract": "bash ./predeploy_contract_check.sh",
    "deploy:safe": "npm run check:api-contract && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260304.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.71.0"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
